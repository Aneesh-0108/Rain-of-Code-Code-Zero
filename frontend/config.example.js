// Copy to config.js and fill values. Do NOT commit real keys if secret (Firebase web keys are public-safe).
export const CONFIG = {
    API_BASE: "http://localhost:5001",
    firebase: {
        apiKey: "YOUR_KEY",
        authDomain: "YOUR_PROJECT.firebaseapp.com",
        projectId: "YOUR_PROJECT",
        appId: "YOUR_APP_ID"
    }
};